[
  {
    "file": "app/Http/Controllers/Api/V1/ActivityController.php",
    "issues": [
      {
        "line": 31,
        "problem": "Uses Request::string() which is removed in Laravel 10.",
        "solution": "Cast input manually, e.g. (string) $request->input('type', '') and guard empty values."
      },
      {
        "line": 32,
        "problem": "Uses Request::string() helper removed in Laravel 10.",
        "solution": "Replace with (string) $request->input('status', '') or similar casting logic."
      },
      {
        "line": 33,
        "problem": "Uses Request::integer() which no longer exists in Laravel 10.",
        "solution": "Use (int) $request->input('area_id') with sensible defaults."
      },
      {
        "line": 35,
        "problem": "Relies on Request::string() for date filters which is unavailable in Laravel 10.",
        "solution": "Access raw input via $request->input('from') and cast to string before parsing."
      },
      {
        "line": 36,
        "problem": "Relies on Request::string() for date filters which is unavailable in Laravel 10.",
        "solution": "Access raw input via $request->input('to') and cast to string before parsing."
      },
      {
        "line": 43,
        "problem": "Uses Request::integer() for pagination which must be replaced in Laravel 10.",
        "solution": "Use (int) $request->input('per_page', 15)."
      },
      {
        "line": 129,
        "problem": "Uses Request::integer() for limit which is removed in Laravel 10.",
        "solution": "Use (int) $request->input('limit', 50) before clamping." 
      }
    ]
  },
  {
    "file": "app/Http/Controllers/Api/V1/AnalyticsController.php",
    "issues": [
      {
        "line": 98,
        "problem": "Uses Request::integer() helper removed in Laravel 10.",
        "solution": "Retrieve days via (int) $request->input('days', 7)."
      }
    ]
  },
  {
    "file": "app/Http/Controllers/Api/V1/LiveDataController.php",
    "issues": [
      {
        "line": 24,
        "problem": "Uses Request::boolean() helper removed in Laravel 10.",
        "solution": "Cast boolean manually via (bool) $request->input('refresh', false)."
      },
      {
        "line": 28,
        "problem": "Uses Request::boolean() helper removed in Laravel 10.",
        "solution": "Use (bool) $request->input('broadcast', true)."
      }
    ]
  },
  {
    "file": "app/Http/Controllers/Api/V1/SmsController.php",
    "issues": [
      {
        "line": 66,
        "problem": "Uses Request::boolean() which is unavailable in Laravel 10.",
        "solution": "Switch to (bool) $request->input('resend', false)."
      }
    ]
  },
  {
    "file": "app/Http/Controllers/Api/V1/AuthController.php",
    "issues": [
      {
        "line": 40,
        "problem": "Uses Request::boolean() helper removed in Laravel 10.",
        "solution": "Replace with (bool) $request->input('remember', false)."
      }
    ]
  },
  {
    "file": "app/Http/Resources/AutomationTaskResource.php",
    "issues": [
      {
        "line": 14,
        "problem": "Resource method type-hints Illuminate\\Http\\Request which causes signature mismatch under Laravel 10.",
        "solution": "Update signature to public function toArray($request)."
      }
    ]
  }
]
